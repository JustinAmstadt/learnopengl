class RawModel {
    vaoID;
    vertexCount;

    /**
     * @param {number} vaoID 
     * @param {number} vertexCount 
     */
    constructor(vaoID, vertexCount) {
        this.vaoID = vaoID;
        this.vertexCount = vertexCount;
    }
}